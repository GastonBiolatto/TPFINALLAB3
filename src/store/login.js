/*import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
    state: {
        users: [],
    },
    getters: {
        users(state) {
            return state.users;
        }
    },
    mutations: {
        
    },
    actions: {
        legear: async function () {
            try {
                const setting = {
                    method: 'GET'
                }

            } catch (error) {
                console.log("error");
            }
        }

    },
    modules: {
    }
})*/